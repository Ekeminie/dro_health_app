import 'package:flutter/material.dart';

class DcotorsPage extends StatelessWidget {
  const DcotorsPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child:Text("DcotorsPage"))
    );
  }
}